# Hexapod-Robot + MuJoCo MJX + Isaac Sim 5.1 강화학습 완전 튜토리얼

이 문서는 `Hexapod-Robot` 저장소 안에 이미 들어있는 URDF/STL 자산을 사용해서 **MuJoCo MJX로 보행의 기초를 먼저 잡고, 그 다음 Isaac Sim 5.1 + Isaac Lab으로 강화학습을 확장하는 파이프라인**을 정리합니다.

범위는 아래까지만 잡습니다.

- `SW/mjx`에서 floating-base MJCF 생성
- MuJoCo MJX 병렬 rollout + CEM으로 기본 tripod gait 탐색
- `HW/urdf` 안의 로봇 설명 파일을 RL용으로 정리
- Isaac Sim 5.1에서 USD로 변환
- Isaac Lab에서 보행용 환경 뼈대 만들기
- PPO(RSL-RL)로 첫 학습 실행
- observation 설계/성능 평가 프레임 정리

이번 문서에서는 **Jetson/STM32 실기 연동은 다루지 않습니다.** 시뮬레이터 안에서 걷는 정책을 먼저 만드는 데만 집중합니다.

---

## 0. 이 저장소에서 바로 쓰는 파일

이번 튜토리얼에서 핵심으로 쓰는 파일은 아래입니다.

- `HW/urdf/package.xml`
- `HW/urdf/meshes/*.stl`
- `HW/urdf/urdf/HEXAPEDAL_URDF.urdf`
- `HW/urdf/urdf/HEXAPEDAL_URDF.xacro`
- `SW/mjx/hexapod_mjx/model.py`
- `SW/mjx/hexapod_mjx/cem.py`
- `SW/mjx/train_tripod_cem.py`

이 URDF에서 확인되는 사실:

- 루트 링크는 `base_link`
- 구동 관절은 총 **18개**
  - `LF_1`, `LM_1`, `LB_1`, `RF_1`, `RM_1`, `RB_1`
  - `LF_2`, `LM_2`, `LB_2`, `RF_2`, `RM_2`, `RB_2`
  - `LF_3`, `LM_3`, `LB_3`, `RF_3`, `RM_3`, `RB_3`
- 관절 제한은 기본적으로 `-2.356194 ~ 2.356194` rad
- `HEXAPEDAL_URDF.urdf` 안에는 `<transmission>` 과 `<gazebo>` 태그가 같이 들어있음
- Jetson, STM32, 릴레이, 배터리 같은 부품도 모두 fixed joint로 묶여 있음

즉, **지금 있는 자산만으로도 MJX baseline과 Isaac Sim 학습용 기본 골격을 둘 다 만들 수 있습니다.**

---

## 1. 왜 Isaac Sim 단독이 아니라 MJX baseline까지 먼저 쓰는가

Isaac Sim 5.1만으로도 로봇을 띄우고 물리 시뮬레이션은 할 수 있습니다. 하지만 보행 강화학습은 보통 아래가 필요합니다.

- 병렬 환경 복제
- observation / reward / reset 관리
- PPO 같은 RL 러너
- 체크포인트 저장/재생

이 부분을 Isaac Lab이 이미 정리해 두었기 때문에, **Hexapod 보행 RL은 최종적으로 Isaac Sim 5.1 + Isaac Lab 조합이 가장 현실적**입니다.

다만 첫 단계에서 바로 PPO로 들어가면 아래가 같이 꼬이기 쉽습니다.

- 기본 서기 자세가 맞는지
- 관절 위상 차를 어떻게 줘야 하는지
- 발 충돌 모델이 너무 무거운지
- 보행 주기/진폭의 대략적인 범위가 어디인지

그래서 이 저장소에서는 **MJX 병렬 rollout으로 저차원 open-loop tripod gait를 먼저 찾고**, 그다음 Isaac Lab PPO로 넘어가는 순서를 추천합니다.

- MJX 단계: 빠르게 걷는 흉내와 충돌/자세 sanity check 확보
- Isaac Lab 단계: command-conditioned policy, reward shaping, terrain generalization 확보

중요한 점은 **MJX baseline이 최종 정책을 대체하는 건 아니라는 것**입니다. 여기서는 보행의 뼈대를 먼저 잡는 용도입니다.

---

## 2. 사전 준비

### 2-1. Isaac Sim 5.1

이 문서는 Isaac Sim 5.1이 아래 경로에 설치되어 있다고 가정합니다.

```bash
~/isaac-sim-5.1
```

실행 확인:

```bash
~/isaac-sim-5.1/isaac-sim.sh
```

---

### 2-2. Isaac Lab 설치

Isaac Sim 5.1 바이너리를 이미 설치한 상태라면, Isaac Lab은 소스 설치가 가장 편합니다.

```bash
export ISAACSIM_PATH="$HOME/isaac-sim-5.1"
export ISAACSIM_PYTHON_EXE="$ISAACSIM_PATH/python.sh"

cd ~
git clone https://github.com/isaac-sim/IsaacLab.git --branch main
cd IsaacLab
ln -s "$ISAACSIM_PATH" _isaac_sim
./isaaclab.sh -i rsl_rl
```

최소 확인:

```bash
cd ~/IsaacLab
./isaaclab.sh -p scripts/environments/list_envs.py --keyword anymal
```

이 단계가 되는 이유는 이후 Hexapod 환경을 만들 때 **ANYmal 예제를 그대로 골격으로 복사**할 수 있기 때문입니다.

---

### 2-3. MuJoCo MJX baseline 코드

저장소에는 기본 보행 뼈대를 찾는 MJX 코드가 이미 들어 있습니다.

- `SW/mjx/hexapod_mjx/model.py`
  - URDF에서 `<transmission>`, `<gazebo>`를 제거
  - floating-base MJCF 생성
  - full mesh 대신 **base box + 발 6개 sphere**로 단순화한 학습용 충돌 모델 생성
- `SW/mjx/hexapod_mjx/cem.py`
  - batched rollout
  - tripod open-loop 파라미터 탐색
  - CEM 기반 파라미터 업데이트
- `SW/mjx/train_tripod_cem.py`
  - 바로 실행하는 CLI

첫 실행 예시는 아래입니다.

```bash
cd ~/Hexapod-Robot
python3 SW/mjx/train_tripod_cem.py \
  --population-size 16 \
  --elite-count 4 \
  --num-iterations 2 \
  --rollout-steps 120 \
  --action-repeat 2 \
  --seed 0 \
  --output-path SW/mjx/artifacts/hexapod_cem_baseline.json
```

실행하면 아래가 생성됩니다.

- `SW/mjx/artifacts/hexapod_floating_base.xml`
- `SW/mjx/artifacts/hexapod_cem_baseline.json`

이 JSON에는 아래가 기록됩니다.

- best score
- best gait 파라미터
- 평균 elite 파라미터
- joint order
- 어떤 MJCF로 돌렸는지

### 3090에서 꼭 확인할 점

MJX가 진짜로 **3090에서 병렬 학습**되려면 JAX가 CUDA 빌드여야 합니다. CUDA-enabled `jaxlib`가 없으면 명령 자체는 돌아가도 **CPU fallback** 으로 실행됩니다.

즉,

- 지금 코드는 CPU에서도 검증 가능
- 하지만 3090을 제대로 쓰려면 **CUDA용 JAX 설치 상태**를 따로 맞춰야 함

이 문서의 순서는 **MJX baseline 확인 → Isaac Lab PPO 확장**입니다.

---

## 3. RL용 URDF 복사본 만들기

Isaac Lab 문서의 URDF 변환 권장 흐름은 다음과 같습니다.

- `<gazebo>` 태그 제거
- `<transmission>` 태그 제거
- 가능하면 fixed joint 병합
- RL 제어용이면 importer 기본 joint drive를 끄거나 약하게 두기

원본을 직접 건드리지 말고 **복사본**을 만듭니다.

```bash
cd ~/Hexapod-Robot/HW/urdf/urdf
python3 - <<'PY'
from pathlib import Path
import re

src = Path("HEXAPEDAL_URDF.urdf").read_text(encoding="utf-8")
src = re.sub(r"\s*<transmission\b.*?</transmission>\s*", "\n", src, flags=re.S)
src = re.sub(r"\s*<gazebo\b.*?</gazebo>\s*", "\n", src, flags=re.S)
Path("HEXAPEDAL_URDF_isaaclab.urdf").write_text(src, encoding="utf-8")
print("wrote HEXAPEDAL_URDF_isaaclab.urdf")
PY
```

결과 파일:

```bash
~/Hexapod-Robot/HW/urdf/urdf/HEXAPEDAL_URDF_isaaclab.urdf
```

### 왜 Jetson/STM32를 따로 지우지 않아도 되나

이번 기본 튜토리얼에서는 변환 시 `--merge-joints` 를 켭니다. 그러면 fixed joint로 붙어 있는 전장 부품은 articulation의 구동 자유도를 늘리지 않고 **베이스 쪽으로 병합**됩니다.

즉,

- Jetson/STM32 실기 연동은 안 쓰더라도
- 지금 URDF를 그대로 출발점으로 써도 되고
- RL의 핵심 자유도 18개만 살리면 됩니다.

다만 추후 성능 최적화 단계에서는 전장 부품 collision/mesh를 더 줄여서 **가벼운 RL 전용 URDF**를 따로 만드는 편이 낫습니다.

---

## 4. URDF를 USD로 변환

Isaac Lab의 `convert_urdf.py` 를 사용하면 Isaac Sim 5.1 importer를 바로 호출할 수 있습니다.

```bash
cd ~/IsaacLab
./isaaclab.sh -p scripts/tools/convert_urdf.py \
  ~/Hexapod-Robot/HW/urdf/urdf/HEXAPEDAL_URDF_isaaclab.urdf \
  ~/Hexapod-Robot/HW/urdf/urdf/HEXAPEDAL_URDF_isaaclab.usd \
  --merge-joints \
  --joint-stiffness 0.0 \
  --joint-damping 0.0 \
  --joint-target-type none \
  --headless
```

권장 옵션 의미:

- `--merge-joints`  
  fixed joint로 붙은 장식/전장 부품을 최대한 합쳐서 articulation 복잡도를 줄임
- `--joint-stiffness 0.0 --joint-damping 0.0 --joint-target-type none`  
  importer가 만든 기본 position drive를 사실상 꺼서, 나중에 RL action이 직접 관절을 제어하기 쉽게 만듦
- `--headless`  
  변환만 하고 GUI 없이 종료

결과 파일:

```bash
~/Hexapod-Robot/HW/urdf/urdf/HEXAPEDAL_URDF_isaaclab.usd
```

---

## 5. Isaac Sim에서 1차 검증

GUI로 먼저 열어서 로봇이 멀쩡한지 확인합니다.

```bash
~/isaac-sim-5.1/isaac-sim.sh
```

확인할 것:

1. USD가 정상적으로 열리는가
2. articulation root가 `base_link` 쪽에 잡히는가
3. 구동 관절이 18개만 남아 있는가
4. 로봇이 중력 아래에서 바로 폭발하지 않는가
5. 발 끝 링크를 식별할 수 있는가

발 끝 이름으로 쓰기 좋은 후보는 아래 6개입니다.

- `LF_foot_3_1`
- `LM_foot_3_1`
- `LB_foot_3_1`
- `RF_foot_3_1`
- `RM_foot_3_1`
- `RB_foot_3_1`

### 처음부터 꼭 맞출 필요 없는 값

초기 기본 자세는 저장소에 따로 RL용 기준값이 없으니, 아래처럼 시작하면 됩니다.

```text
.*_1 = 0.0 rad
.*_2 = 0.6 rad
.*_3 = -1.2 rad
```

이 값은 **추천 시작점**이지 정답은 아닙니다. GUI에서 서 있는 자세가 더 자연스럽게 보이도록 조정하면 됩니다.

---

## 6. Isaac Lab에서 어떤 예제를 베이스로 잡을까

Hexapod 보행의 첫 버전은 **ANYmal-C direct locomotion 예제**를 베이스로 잡는 게 가장 쉽습니다.

참고할 파일:

- `source/isaaclab_tasks/isaaclab_tasks/direct/anymal_c/anymal_c_env.py`
- `source/isaaclab_tasks/isaaclab_tasks/direct/anymal_c/anymal_c_env_cfg.py`

이 예제는 이미 아래가 정리돼 있습니다.

- velocity command 추종 구조
- 관절 action 적용
- reward 계산
- contact 기반 종료 조건
- PPO 학습 스크립트와 연결

즉 **보행 목적 자체가 비슷해서 복사 출발점으로 좋습니다.**

---

## 7. Hexapod 환경 파일 권장 배치

Isaac Lab 쪽에는 아래처럼 새 태스크를 두는 걸 권장합니다.

```text
IsaacLab/
└─ source/isaaclab_tasks/isaaclab_tasks/direct/hexapod/
   ├─ __init__.py
   ├─ hexapod_env.py
   ├─ hexapod_env_cfg.py
   └─ agents/
      └─ rsl_rl_ppo_cfg.py
```

가장 단순한 방법은 `direct/anymal_c` 폴더를 복사한 뒤 이름만 `hexapod`로 바꾸고, 아래 항목만 먼저 바꾸는 것입니다.

---

## 8. Hexapod 태스크에서 최소한 바꿔야 하는 것

### 8-1. 로봇 asset 경로

ANYmal 자리에 방금 만든 USD를 넣습니다.

```text
~/Hexapod-Robot/HW/urdf/urdf/HEXAPEDAL_URDF_isaaclab.usd
```

### 8-2. action 차원

Hexapod는 18 자유도이므로 action 차원은 **18**입니다.

권장 action 순서 예시:

```text
LF_1 LF_2 LF_3
LM_1 LM_2 LM_3
LB_1 LB_2 LB_3
RF_1 RF_2 RF_3
RM_1 RM_2 RM_3
RB_1 RB_2 RB_3
```

중요한 건 순서 자체보다 **관측/리워드/정책 전체에서 같은 순서를 일관되게 유지하는 것**입니다.

### 8-3. 관절 기본자세

처음엔 아래처럼 leg type별 공통값으로 두면 됩니다.

```text
hip yaw  (*_1):  0.0
hip pitch(*_2):  0.6
knee     (*_3): -1.2
```

### 8-4. 발 contact body 이름

contact sensor는 아래 6개 발 끝 링크를 기준으로 잡는 게 편합니다.

```text
LF_foot_3_1
LM_foot_3_1
LB_foot_3_1
RF_foot_3_1
RM_foot_3_1
RB_foot_3_1
```

### 8-5. 종료 조건

가장 단순한 첫 종료 조건은 아래 둘이면 충분합니다.

- `base_link` 가 바닥에 강하게 닿음
- episode time-out

처음부터 thigh/shank/foot 외 접촉 패널티를 과하게 넣으면 학습이 더 꼬일 수 있으니, **첫 버전은 base crash만 종료로 두는 게 낫습니다.**

---

## 9. 관측(observation) 추천 시작점

첫 버전은 ANYmal과 비슷하게 아래로 시작하면 됩니다.

- body frame 선속도 3
- body frame 각속도 3
- projected gravity 3
- 목표 속도 command 3
- joint position error 18
- joint velocity 18
- 이전 action 18

합계:

```text
3 + 3 + 3 + 3 + 18 + 18 + 18 = 66 차원
```

이 66차원으로 먼저 평지 보행을 학습시키고, 그 다음에 아래를 추가하세요.

- base height
- foot contact state
- terrain height scan
- IMU/센서 노이즈 모델

---

## 10. reward 추천 시작점

첫 버전 reward는 ANYmal flat locomotion과 같은 철학으로 두는 게 좋습니다.

핵심 항목:

- `x/y` 선속도 추종 보상
- `yaw rate` 추종 보상
- `z` 속도 패널티
- 불필요한 `roll/pitch` 패널티
- action rate 패널티
- joint torque 패널티
- 발의 air-time 보상

실전 팁:

- **처음엔 예쁜 보행보다 넘어지지 않고 목표 속도 따라가는 것**이 우선
- foot slip, joint symmetry, tripod gait 보상은 **두 번째 단계**에서 추가
- reward를 많이 넣는 것보다, **단순 reward + 좋은 초기자세**가 훨씬 잘 먹힙니다

---

## 11. 첫 학습 실행

환경 등록을 끝냈다고 가정하면, 학습 명령은 아래 형태가 됩니다.

다만 **이 PPO를 돌리기 전에 MJX baseline이 최소한 앞으로 가는 점수는 내는지** 먼저 확인하는 게 좋습니다. MJX 단계에서조차 서기/전진이 안 되면, Isaac Lab에서 reward를 만지는 것보다 **자세, 충돌, 기본 gait 주기**를 먼저 고치는 편이 빠릅니다.

```bash
cd ~/IsaacLab
./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/train.py \
  --task Isaac-Hexapod-Flat-Direct-v0 \
  --num_envs 1024 \
  --max_iterations 3000 \
  --headless
```

### 3090 기준 권장 시작값

네 GPU가 RTX 3090 24GB라면 첫 시도는 아래가 무난합니다.

- MJX baseline: 먼저 작은 population으로 smoke test, 그 다음 population/iteration 확대
- Isaac Lab PPO: `num_envs=1024` 로 시작
- 안정적이면 `1536` 또는 `2048`로 증가
- 처음부터 rough terrain 대신 **flat terrain** 으로 시작

왜 이렇게 가냐면,

- Hexapod는 18 DOF라 action/observation이 단순한 편은 아니고
- full mesh 기반 자산이면 collision 비용도 적지 않아서
- MJX에서 기초 gait를 잡아두면 Isaac Lab reward debugging 시간이 크게 줄기 때문입니다.


---

## 12. 체크포인트 재생

학습이 끝나거나 중간 checkpoint가 생기면 재생은 아래처럼 합니다.

```bash
cd ~/IsaacLab
./isaaclab.sh -p scripts/reinforcement_learning/rsl_rl/play.py \
  --task Isaac-Hexapod-Flat-Direct-v0 \
  --num_envs 1 \
  --checkpoint /path/to/checkpoint.pt
```

`play.py`는 정책을 읽은 뒤 JIT/ONNX export도 같이 해 주므로, 나중에 실기 제어 쪽으로 넘길 때 출발점으로 쓰기 좋습니다.

---

## 13. 가장 먼저 성공으로 봐야 하는 기준

첫 번째 성공 기준은 거창할 필요 없습니다.

1. 평지에서 spawn 직후 바로 넘어지지 않음
2. 5~10초 정도 base contact 없이 버팀
3. 전진 속도 command에 어느 정도 반응함
4. 학습 로그에서 reward가 계속 상승함

이게 되면 그 다음 단계는 아래 순서가 좋습니다.

- flat terrain 안정화
- rough terrain 추가
- domain randomization
- contact/foot slip 보상 추가
- 실기 joint 범위와 토크 한계에 맞춘 보정

---

## 14. 이 저장소 기준으로 가장 중요한 요약

### 지금 바로 써도 되는 것

- `HW/urdf/urdf/HEXAPEDAL_URDF.urdf`
- `HW/urdf/meshes/*.stl`
- 18개 구동 관절 이름

### RL용으로 한 번 정리해야 하는 것

- `<transmission>` 제거
- `<gazebo>` 제거
- USD 변환
- RL 기본자세 설정
- Isaac Lab custom task 생성

### 굳이 지금 안 해도 되는 것

- Jetson 실기 연동
- STM32 통신
- LiDAR / D435 / GPS / LoRa 모델링
- 거친 지형 학습

---

## 15. 추천 진행 순서

아무 생각 없이 딱 이 순서로 가면 됩니다.

1. `python3 SW/mjx/train_tripod_cem.py ...` 로 MJX smoke test 실행
2. `SW/mjx/artifacts/hexapod_cem_baseline.json` 에서 gait 주기/진폭 범위 확인
3. `HEXAPEDAL_URDF_isaaclab.urdf` 생성
4. `HEXAPEDAL_URDF_isaaclab.usd` 생성
5. Isaac Sim GUI에서 자세/충돌 확인
6. Isaac Lab의 ANYmal-C direct task 복사
7. asset path, joint names, foot names, default pose만 수정
8. flat terrain + PPO로 첫 학습
9. reward/초기자세 튜닝
10. rough terrain으로 확장

---

## 16. 1차 결론

이 저장소는 이미 **URDF + STL + 18자유도 관절 구조**가 준비되어 있어서, Jetson 쪽을 건드리지 않아도 **Hexapod 기본 보행을 MJX와 Isaac Sim 양쪽에서 시작하기에 충분한 상태**입니다.  
가장 빠른 순서는 **MJX로 open-loop tripod gait 기초 확보 → URDF 정리 → USD 변환 → Isaac Lab 보행 task 생성 → PPO 학습** 입니다.

아래 17장부터는 이 baseline 위에서 **어떤 observation 구성이 실제로 더 좋은 보행을 만드는지 비교·평가하는 프레임**을 정리합니다.


---

## 17. 어떤 observation/input를 줘야 보행이 제일 잘 되나

여기서 제일 먼저 바로잡아야 할 점이 하나 있습니다.

**"입력을 많이 넣을수록 보행이 무조건 좋아지는 건 아닙니다."**

보행 RL에서는 입력이 늘수록 아래 문제가 같이 커집니다.

- 학습이 느려짐
- 불필요한 상관관계를 주워먹음
- flat terrain에서만 잘 되고 terrain이 바뀌면 망가짐
- 실기 이식성이 떨어짐

그래서 목표는 **가장 많은 입력**이 아니라,
**현재 보행 과제에 필요한 최소 입력 + 정말 성능을 올리는 추가 입력**을 찾는 것입니다.

### 17-1. Hexapod 보행 입력은 4단계로 나누면 편하다

#### A. 코어 proprioception 입력
평지 보행이든 거친 지형이든 거의 항상 기본으로 들어가는 입력입니다.

- `projected_gravity_b` : 몸체 기울기 추정
- `root_ang_vel_b` : 몸체 각속도
- `root_lin_vel_b` : 몸체 선속도
- `commanded velocity` : 목표 전진/횡이동/yaw 속도
- `joint_pos - default_joint_pos` : 관절 자세 오차
- `joint_vel` : 관절 속도
- `previous_action` : action 변화율과 actuator inertia 반영

이 묶음은 **대부분의 보행 RL baseline에서 가장 먼저 검토하는 1순위 observation** 입니다.


#### B. 접촉(contact) 인식 입력
Hexapod가 사족보다 이득을 보기 쉬운 부분입니다.

- 각 발의 contact on/off
- 발별 contact force 또는 normal force
- 발별 air-time / stance-time
- slip 여부 또는 foot tangential velocity

이 묶음은 **넘어짐 감소, tripod gait 안정화, 발 헛디딤 감지**에 도움이 됩니다.

#### C. 지형(terrain) 인식 입력
flat에서는 없어도 되지만, rough terrain에서는 성능 차이가 큽니다.

- 발 주변 local height scan
- terrain normal / slope
- 발 예정 착지점 주변의 높이 편차
- body 기준 전방 높이맵

이 묶음은 **계단, 턱, 경사, uneven terrain**에서 특히 중요합니다.

#### D. 태스크 문맥(task context) 입력
보행 목적이 단순 속도추종인지, 경로추종인지에 따라 달라집니다.

- 목표 heading
- 목표 waypoint까지 상대 위치
- gait phase / clock signal
- desired foothold region

이 묶음은 **navigation / foothold planning / command-conditioned gait**에서 유효합니다.

---

## 18. 입력 후보별 추천 우선순위

### 18-1. 가장 먼저 돌릴 기본 조합

**Observation Set A — Flat Locomotion Baseline**

- `projected_gravity_b`
- `root_ang_vel_b`
- `root_lin_vel_b`
- `commanded velocity`
- `joint_pos error`
- `joint_vel`
- `previous_action`

이 조합을 먼저 추천하는 이유:

- Isaac Lab ANYmal 계열 기본 철학과 가장 가깝고
- 현재 Hexapod URDF만으로 바로 구성 가능하며
- 쓸데없는 센서 없이도 보행 baseline을 만들 수 있기 때문입니다.

즉, **첫 baseline은 보통 A부터 시작하는 편이 낫지만**, 처음부터 rough terrain / foothold planning / 특수 gait 제약이 목표라면 contact나 terrain 입력을 같이 넣는 편이 더 맞을 수 있습니다.


### 18-2. 두 번째로 붙일 조합

**Observation Set B — A + Foot Contact**

- Set A 전체
- 발 6개의 contact on/off
- 발별 air-time 또는 stance-time

이 조합은 아래를 보기 좋습니다.

- body stability 개선 여부
- 보행 duty factor 개선 여부
- slip 감소 여부
- reset 빈도 감소 여부

Hexapod는 다리가 6개라서 **contact 정보가 gait organization에 주는 이득**이 큰 편입니다.

### 18-3. 거친 지형으로 갈 때 붙일 조합

**Observation Set C — B + Terrain Height Scan**

- Set B 전체
- body 전방 또는 발 주변 local height scan
- slope / terrain normal

이건 flat에서는 과할 수 있지만, **rough terrain·경사·턱 넘기 비중이 큰 과제에서는 매우 강한 후보**입니다.


### 18-4. 마지막에 검토할 조합

**Observation Set D — C + Slip/Friction/Foot Force 정교화**

- Set C 전체
- foot normal force
- tangential slip estimate
- friction proxy 또는 contact quality signal

이 조합은 이론상 좋지만, 처음부터 넣으면 학습이 복잡해집니다.
따라서 **성능 최적화 단계**에서만 넣는 게 낫습니다.

---

## 19. 오해하기 쉬운 입력들

### 19-1. 절대 위치(x, y, world position)

사용자가 예시로 든 "위치"는 **그대로 넣으면 항상 좋은 입력이 아닙니다.**

- **속도추종 보행 task** 에서는 절대 world position이 거의 필요 없습니다.
- 오히려 특정 맵 원점/초기 위치에 overfit할 수 있습니다.

권장:

- 속도추종이면 `root_lin_vel_b`, `commanded velocity` 중심
- waypoint/navigation이면 `goal relative position` 사용
- 절대 world 좌표는 마지막까지 안 넣는 쪽이 낫습니다.

### 19-2. 모든 관절 절대각 + 링크 절대 pose 전부 넣기

가능은 하지만 대체로 비효율적입니다.

- 이미 `joint_pos error`, `joint_vel`, `projected_gravity`, `root velocities` 로 핵심 상태를 설명할 수 있고
- 링크 절대 pose를 잔뜩 넣으면 차원만 커지고 일반화가 나빠질 수 있습니다.

### 19-3. contact force는 binary contact보다 항상 낫다

그렇지 않습니다.

- 초기 학습에서는 binary contact만으로도 충분한 경우가 많고
- noisy force 값은 policy를 흔들 수 있습니다.

그래서 보통은:

1. `contact on/off`
2. `air-time / stance-time`
3. 그 다음 `contact force`

순서가 더 낫습니다.

---

## 20. 성능 평가표는 이렇게 짜는 게 좋다

입력 조합의 우열은 한 지표로 보면 안 됩니다.
최소한 아래 6개 축으로 평가해야 합니다.

| 평가 축 | 의미 | 예시 측정값 |
| --- | --- | --- |
| 속도 추종 성능 | 목표 명령을 얼마나 잘 따르는가 | `vx RMSE`, `vy RMSE`, `yaw rate RMSE` |
| 안정성 | 넘어지지 않고 버티는가 | episode survival time, reset rate, body roll/pitch RMS |
| 에너지 효율 | 같은 성능을 더 적은 effort로 내는가 | torque^2 적분, cost of transport |
| 접촉 품질 | 발이 미끄러지거나 헛디디지 않는가 | slip distance, false contact rate, duty factor consistency |
| 회복력 | 외란 후 다시 자세를 회복하는가 | push recovery success rate, recovery time |
| 일반화 | terrain/command가 바뀌어도 유지되는가 | train terrain 외 성능 유지율 |

### 20-1. 추천 종합 점수 예시

평지 baseline 비교에서는 아래처럼 가중합을 쓰면 됩니다.

```text
Total Score
= 0.30 * Velocity Tracking
+ 0.25 * Stability
+ 0.15 * Energy Efficiency
+ 0.10 * Slip Quality
+ 0.10 * Recovery
+ 0.10 * Generalization
```

rough terrain 중심이면 아래처럼 바꾸는 게 더 낫습니다.

```text
Total Score
= 0.20 * Velocity Tracking
+ 0.25 * Stability
+ 0.15 * Energy Efficiency
+ 0.15 * Slip Quality
+ 0.15 * Recovery
+ 0.10 * Generalization
```

즉, **최적 입력은 절대값 하나가 아니라 평가 목적에 따라 달라집니다.**

### 20-2. 실제 winner를 고르는 판정 규칙

입력 조합 A/B/C의 우열은 아래처럼 정하면 해석이 덜 흔들립니다.

| 판정 규칙 | 권장 기준 |
| --- | --- |
| seed 평균 비교 | 최소 3개, 가능하면 5개 seed 평균 사용 |
| 종합 점수 우세 | 다른 후보보다 **5점 이상** 높을 때만 "의미 있는 우세"로 판정 |
| stability 하한 | survival time, reset rate가 baseline보다 **나빠지면 탈락** |
| 효율 하한 | torque/cost of transport가 **15% 이상 악화**되면 탈락 후보 |
| 복잡도 tie-break | 종합 점수 차이가 **3점 이내**면 더 단순한 observation set 채택 |
| rough terrain 우선 조건 | flat 점수는 약간 낮아도 rough terrain/generalization이 크게 좋으면 채택 가능 |

### 20-3. 비교표 템플릿

실험 후에는 아래처럼 **후보별로 같은 표**를 채워서 비교하세요.

| 후보 | Tracking | Stability | Energy | Slip | Recovery | Generalization | Total | 판정 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Set A |  |  |  |  |  |  |  |  |
| Set B |  |  |  |  |  |  |  |  |
| Set C |  |  |  |  |  |  |  |  |
| Set D |  |  |  |  |  |  |  |  |

이 표를 채우면 "좋아 보인다" 수준이 아니라, **어느 축에서 이겼는지/졌는지**로 입력 조합을 판정할 수 있습니다.

---

## 21. 추천 비교 실험 설계

### 21-1. 고정해야 하는 것

입력 조합만 비교하려면 아래는 고정해야 합니다.

- 같은 reward 구조
- 같은 network 크기
- 같은 PPO 하이퍼파라미터
- 같은 domain randomization 범위
- 같은 training iteration
- 같은 seed 개수
- 같은 terrain 세트

### 21-2. 최소 비교 세트

첫 실험은 아래 4개면 충분합니다.

| 실험 ID | 입력 구성 | 목적 |
| --- | --- | --- |
| E1 | Set A | flat baseline |
| E2 | Set A + contact binary | contact 정보 가치 확인 |
| E3 | Set A + contact binary + air-time | gait timing 정보 가치 확인 |
| E4 | Set C | rough terrain 확장성 확인 |

### 21-3. 추천 실행 순서

1. **E1**: flat terrain에서 baseline 학습
2. **E2**: contact on/off 추가
3. **E3**: air-time/stance-time 추가
4. **E4**: rough terrain에서 terrain scan 추가
5. 필요하면 **E5**: force/slip proxy 추가

핵심은 **flat도 못 걷는데 terrain scan부터 넣지 않는 것**입니다.

---

## 22. 지금 Hexapod 기준으로 내가 고르면

현재 저장소와 Isaac Sim 5.1 + Isaac Lab 기준에서, 가장 먼저 실험할 observation은 아래입니다.

### 1순위: Flat Baseline

- `projected_gravity_b`
- `root_ang_vel_b`
- `root_lin_vel_b`
- `commanded velocity (vx, vy, wz)`
- `18개 joint position error`
- `18개 joint velocity`
- `previous_action`

### 2순위: Contact-Aware Baseline

- 1순위 전체
- `LF/RF/LM/RM/LB/RB` foot contact binary
- 발별 air-time

### 3순위: Rough Terrain Candidate

- 2순위 전체
- body 전방 local height scan 또는 발 주변 높이 샘플

이 순서를 추천하는 이유는:

- 현재 저장소 자산만으로 바로 구성 가능하고
- flat → contact-aware → terrain-aware 순으로 원인 분석이 쉽고
- 어느 입력이 성능 향상에 실제로 기여했는지 분리하기 좋기 때문입니다.

---

## 23. 반드시 남겨야 할 실험 로그

입력 조합 비교는 학습 curve만 보면 안 됩니다. 아래를 각 실험마다 남기세요.

- best reward
- 최종 100 episode 평균 reward
- velocity tracking error
- 평균 생존시간
- reset rate
- slip metric
- torque usage
- push recovery success rate
- terrain별 성능 표
- seed별 평균과 표준편차

즉, **"가장 잘된 체크포인트 1개"** 가 아니라,
**"같은 예산에서 평균적으로 가장 안정적인 observation set"** 을 찾는 식으로 봐야 합니다.

---

## 24. 바로 다음 단계 제안

지금 방향에서 가장 실용적인 다음 단계는 아래입니다.

1. `Set A` 로 flat velocity tracking baseline 확정
2. `Set A + contact binary + air-time` 비교
3. 성능표 기준으로 어떤 축이 실제로 좋아졌는지 확인
4. 그 다음에만 terrain height scan 추가

요약하면:

- **평지 baseline 전에는 terrain 입력 과투자 금지**
- **absolute world position은 기본 observation에서 제외**
- **contact는 초반에 넣어볼 가치가 높음**
- **force/slip 정교 입력은 마지막 단계**

---

## 25. 지금 확인이 꼭 필요한 재질문

방향을 더 정확히 잡으려면 아래는 꼭 정해야 합니다.

1. **목표 보행이 뭔가?**
   - 평지 고속 주행
   - 저속 안정 보행
   - rough terrain
   - 계단/턱 넘기
   - waypoint 추종

2. **정책 출력이 뭔가?**
   - joint position target
   - joint velocity target
   - torque / effort
   - CPG + residual

3. **평가 1순위가 뭔가?**
   - 속도 추종
   - 안 넘어짐
   - 에너지 효율
   - terrain generalization
   - 외란 회복

4. **gait를 자유학습시킬 건가, tripod gait bias를 줄 건가?**
   - 자유학습
   - gait phase/clock 입력 포함
   - tripod 제약 일부 포함

5. **실기 이식까지 염두에 두는가?**
   - 그렇다면 absolute/global 정보보다 proprioception + contact 중심이 더 안전합니다.

이 5개가 정해지면, 그 다음엔 observation set을 훨씬 더 날카롭게 줄일 수 있습니다.
